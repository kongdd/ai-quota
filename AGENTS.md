# AGENTS.md

## User Context

- 用户是 20 年码龄资深程序员，偏好简洁、优雅、易懂、规范的代码。

## Repository

- Project: `ai-quota`
- Language/runtime: TypeScript, Node.js ESM
- Package manager: npm
- Source code: `src/`
- Documentation/images: `README.md`, `docs/`
- Build output: `dist/` generated by `npm run build`

## Commands

Always prefer `rtk` for shell commands.

```bash
rtk npm run build
rtk git diff --check
rtk git status --short
```

Useful local aliases/environment:

```bash
alias Rscript=/opt/miniforge3/envs/r4.5/bin/Rscript
alias R=/opt/miniforge3/envs/r4.5/bin/R
alias julia=/home/kong/.local/bin/julia
```

## Tooling Preferences

- Prefer MCP `sqz_read_file` for reading larger source/config/docs files.
- Prefer `rtk` for command output, especially `git`, `rg`, `ls`, build, and test commands.
- Prefer codebase-memory MCP graph tools for code discovery before grep:
  1. `search_graph`
  2. `trace_path`
  3. `get_code_snippet`
  4. `query_graph`
  5. `get_architecture`
- Fall back to `rg` for literals, configs, shell scripts, docs, and when graph results are insufficient.

## Coding Style

- Keep changes small and localized.
- Reuse existing helpers and data structures before adding abstractions.
- Avoid unrelated refactors and formatting churn.
- Use TypeScript strictness instead of weakening types.
- Preserve existing CLI behavior unless the task explicitly changes it.
- Do not overwrite unrelated user changes in the worktree.

## Project-Specific Notes

- `ai-quota` and `api-usage` share DeepSeek accounting through `src/deepseek.ts`.
- Generic balance-based daily/weekly ledger logic lives in `src/balance-ledger.ts`; provider files should only fetch/parse their own API balance and pass it into the ledger.
- DeepSeek API only exposes current balance. Local state in `~/.config/ai-quota/api-usage.json` is the source of truth for daily/weekly budget accounting.
- Critical DeepSeek state fields must be persisted:
  - account-level `last_balance`, `updated_at`
  - day/week `baseline_balance`, `created_at`, `spent`, `last_balance`, `updated_at`
- Balance drops should increase `spent`; top-ups should update `last_balance` without reducing recorded `spent`.
- Keep `week` as the ISO state key, e.g. `2026-W27`; use `weekStart` for user-facing dates, e.g. `2026-06-29`.

## Verification

For TypeScript/code changes, run:

```bash
rtk npm run build
rtk git diff --check
```

For DeepSeek accounting changes, also verify with mocked `fetch` scenarios:

- multiple runs in one day
- balance top-up
- cross-day usage
- week accumulation
